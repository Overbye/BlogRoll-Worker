name: Deploy
on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    # 避开整点的 GitHub Actions 调度高峰；时间为 UTC。
    - cron: "17 4,16 * * *"

permissions:
  contents: read

concurrency:
  group: blogroll-production
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Generate RSS data
        run: npm run gen
      - name: Build site
        run: npm run build
      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CF_WORKERS_TOKEN }}
          command: deploy

